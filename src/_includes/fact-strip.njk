{% macro factStrip(items) %}
<div class="fact-strip">
  {% for item in items %}
  <div class="fact-strip__item">
    <div class="fact-strip__label">{{ item.label }}</div>
    <div class="fact-strip__value">{{ item.value }}</div>
  </div>
  {% endfor %}
</div>
{% endmacro %}
